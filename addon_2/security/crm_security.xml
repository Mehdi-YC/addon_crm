<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="group_crm_user" model="res.groups">
        <field name="name">CRM User</field>
        <field name="category_id" ref="base.module_category_hidden"/>
    </record>

    <record id="group_crm_manager" model="res.groups">
        <field name="name">CRM Manager</field>
        <field name="implied_ids" eval="[(4, ref('group_crm_user'))]"/>
    </record>

    <!-- Users only see their own leads -->
    <record id="rule_crm_lead_own" model="ir.rule">
        <field name="name">CRM Lead: own leads</field>
        <field name="model_id" ref="model_crm_lead"/>
        <field name="domain_force">[('user_id', 'in', [user.id, False])]</field>
        <field name="groups" eval="[(4, ref('group_crm_user'))]"/>
        <field name="perm_read"   eval="True"/>
        <field name="perm_write"  eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Managers see all leads -->
    <record id="rule_crm_lead_manager" model="ir.rule">
        <field name="name">CRM Lead: all leads for managers</field>
        <field name="model_id" ref="model_crm_lead"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_crm_manager'))]"/>
    </record>
</odoo>
