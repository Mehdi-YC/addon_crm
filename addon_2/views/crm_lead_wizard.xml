<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data>

<!-- Wizard form view -->
<record id="crm_import_wizard_form" model="ir.ui.view">
    <field name="name">crm.import.wizard.form</field>
    <field name="model">crm.import.wizard</field>
    <field name="arch" type="xml">
        <form string="Import Leads from API">
            <sheet>
                <group>
                    <field name="category_id"/>
                    <field name="import_count"
                           readonly="1"
                           string="Leads found"/>
                </group>

                <!-- Preview table — readonly, populated by onchange -->
                <field name="preview_ids" readonly="1">
                    <list>
                        <field name="name"/>
                        <field name="phone"/>
                        <field name="email"/>
                        <field name="expected_revenue"/>
                    </list>
                </field>
            </sheet>

            <footer>
                <button name="action_import"
                        string="Import All"
                        type="object"
                        class="btn-primary"
                        invisible="import_count == 0"/>
                <button string="Cancel"
                        class="btn-secondary"
                        special="cancel"/>
            </footer>
        </form>
    </field>
</record>

<!-- Action to open wizard as a popup dialog -->
<record id="action_import_leads_wizard" model="ir.actions.act_window">
    <field name="name">Import Leads from API</field>
    <field name="res_model">crm.import.wizard</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
</record>

</data>
</odoo>
